<Window x:Class="Proc.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Proc - Settings"
        Width="340" Height="340"
        ResizeMode="NoResize"
        Background="{DynamicResource ThemeBg}"
        WindowStartupLocation="CenterScreen"
        Icon="/Proc.ico"
        KeyDown="Window_KeyDown">

    <Grid Margin="20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Settings" Foreground="{DynamicResource ThemeAccent}"
                   FontFamily="Consolas" FontSize="14" FontWeight="Bold"/>

        <CheckBox x:Name="StartupCheckBox" Grid.Row="2"
                  Content="Windows startup" Foreground="{DynamicResource ThemeText}"
                  FontFamily="Consolas" FontSize="12"
                  Checked="StartupCheckBox_Changed" Unchecked="StartupCheckBox_Changed">
            <CheckBox.Style>
                <Style TargetType="CheckBox">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="CheckBox">
                                <StackPanel Orientation="Horizontal">
                                    <Border x:Name="Bd" Width="16" Height="16"
                                            BorderBrush="{DynamicResource ThemeCheckBorder}"
                                            BorderThickness="1" CornerRadius="2"
                                            Background="{DynamicResource ThemeCheckBg}"
                                            VerticalAlignment="Center" Margin="0,0,8,0">
                                        <TextBlock x:Name="Check" Text="&#x2713;"
                                                   Foreground="{DynamicResource ThemeAccent}"
                                                   FontSize="12" HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" Margin="0,-2,0,0"
                                                   Visibility="Collapsed"/>
                                    </Border>
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource ThemeAccent}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeHover}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </CheckBox.Style>
        </CheckBox>

        <CheckBox x:Name="ShowTitleCheckBox" Grid.Row="4"
                  Content="Show window title" Foreground="{DynamicResource ThemeText}"
                  FontFamily="Consolas" FontSize="12"
                  Checked="ShowTitleCheckBox_Changed" Unchecked="ShowTitleCheckBox_Changed">
            <CheckBox.Style>
                <Style TargetType="CheckBox">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="CheckBox">
                                <StackPanel Orientation="Horizontal">
                                    <Border x:Name="Bd" Width="16" Height="16"
                                            BorderBrush="{DynamicResource ThemeCheckBorder}"
                                            BorderThickness="1" CornerRadius="2"
                                            Background="{DynamicResource ThemeCheckBg}"
                                            VerticalAlignment="Center" Margin="0,0,8,0">
                                        <TextBlock x:Name="Check" Text="&#x2713;"
                                                   Foreground="{DynamicResource ThemeAccent}"
                                                   FontSize="12" HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" Margin="0,-2,0,0"
                                                   Visibility="Collapsed"/>
                                    </Border>
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource ThemeAccent}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeHover}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </CheckBox.Style>
        </CheckBox>

        <CheckBox x:Name="RunAsAdminCheckBox" Grid.Row="6"
                  Content="Run as administrator" Foreground="{DynamicResource ThemeText}"
                  FontFamily="Consolas" FontSize="12"
                  Checked="RunAsAdminCheckBox_Changed" Unchecked="RunAsAdminCheckBox_Changed">
            <CheckBox.Style>
                <Style TargetType="CheckBox">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="CheckBox">
                                <StackPanel Orientation="Horizontal">
                                    <Border x:Name="Bd" Width="16" Height="16"
                                            BorderBrush="{DynamicResource ThemeCheckBorder}"
                                            BorderThickness="1" CornerRadius="2"
                                            Background="{DynamicResource ThemeCheckBg}"
                                            VerticalAlignment="Center" Margin="0,0,8,0">
                                        <TextBlock x:Name="Check" Text="&#x2713;"
                                                   Foreground="{DynamicResource ThemeAccent}"
                                                   FontSize="12" HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" Margin="0,-2,0,0"
                                                   Visibility="Collapsed"/>
                                    </Border>
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource ThemeAccent}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeHover}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </CheckBox.Style>
        </CheckBox>

        <!-- Theme selection -->
        <TextBlock Grid.Row="8" Text="Theme" Foreground="{DynamicResource ThemeAccent}"
                   FontFamily="Consolas" FontSize="14" FontWeight="Bold"/>

        <ComboBox x:Name="ThemeComboBox" Grid.Row="10"
                  FontFamily="Consolas" FontSize="12"
                  SelectionChanged="ThemeComboBox_SelectionChanged">
            <ComboBox.Style>
                <Style TargetType="ComboBox">
                    <Setter Property="Foreground" Value="{DynamicResource ThemeText}"/>
                    <Setter Property="Background" Value="{DynamicResource ThemeCheckBg}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource ThemeCheckBorder}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Padding" Value="6,4"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBox">
                                <Grid>
                                    <Border x:Name="Border" Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="3">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="24"/>
                                            </Grid.ColumnDefinitions>
                                            <ContentPresenter Grid.Column="0" Margin="{TemplateBinding Padding}"
                                                              Content="{TemplateBinding SelectionBoxItem}"
                                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                              VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="&#x25BC;" FontSize="9"
                                                       Foreground="{DynamicResource ThemeSubText}"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                    <ToggleButton x:Name="ToggleButton" Grid.ColumnSpan="2"
                                                  Focusable="False" ClickMode="Press"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Background="Transparent" BorderThickness="0" Opacity="0"/>
                                    <Popup x:Name="Popup" Placement="Bottom"
                                           IsOpen="{TemplateBinding IsDropDownOpen}"
                                           AllowsTransparency="True" Focusable="False"
                                           PopupAnimation="Slide">
                                        <Border Background="{DynamicResource ThemeSurface}"
                                                BorderBrush="{DynamicResource ThemeCheckBorder}"
                                                BorderThickness="1" CornerRadius="3"
                                                MinWidth="{TemplateBinding ActualWidth}"
                                                MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                            <ScrollViewer>
                                                <StackPanel IsItemsHost="True"/>
                                            </ScrollViewer>
                                        </Border>
                                    </Popup>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource ThemeAccent}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ComboBox.Style>
            <ComboBox.ItemContainerStyle>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="{DynamicResource ThemeText}"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="FontFamily" Value="Consolas"/>
                    <Setter Property="FontSize" Value="12"/>
                    <Setter Property="Padding" Value="6,4"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <StackPanel Orientation="Horizontal">
                                        <Border x:Name="ColorDot" Width="12" Height="12" CornerRadius="6"
                                                Margin="0,0,8,0" VerticalAlignment="Center"
                                                Background="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeHover}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ComboBox.ItemContainerStyle>
        </ComboBox>
    </Grid>
</Window>
